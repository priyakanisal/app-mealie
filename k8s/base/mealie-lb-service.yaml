apiVersion: v1
kind: Service
metadata:
  namespace: app-mealie
  labels:
    app: mealie-deployment
    name: app-mealie
  name: mealie-lb
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: mealie-deployment
  type: LoadBalancer
